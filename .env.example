# No frontend API env var is required.
# Local development uses the Vite proxy to forward `/api/*` to `http://localhost:5000/api/*`.
# Netlify production should define BACKEND_BASE_URL in the site environment settings.
